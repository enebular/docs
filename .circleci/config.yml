version: 2 # use CircleCI 2.0
jobs: # a collection of steps
  build: # runs not using Workflows must have a `build` job as entry point
    working_directory: ~/mern-starter # directory where steps will run
    docker: # run the steps with Docker
      - image: circleci/node:4.8.2 # ...with this image as the primary container; this is where all `steps` will run
      - image: mongo:3.4.4 # and this image as the secondary service container
    steps: # a collection of executable commands 
      - checkout # special step to check out source code to working directory
      - run:
          name: update-npm 
          command: 'sudo npm install -g npm@latest'
      - restore_cache: # special step to restore the dependency cache 
          key: dependency-cache-{{ checksum "package.json" }}
      - run:
          name: install markdown link checker
          command: 'npm install -g markdown-link-check@3.7.2'
      - run:
          name: find
          command: ' find . -name \*.md -exec markdown-link-check {} -q -c config.json \; 2>error_log'
      - run:
          name: log check
          command: 'if [ -s ./error_log ]; then echo "Broken links have been found in the documents."; exit 1; fi'